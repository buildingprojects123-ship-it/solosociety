# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Database (PostgreSQL)
# For local development, you can use a local PostgreSQL or get a free one from Vercel/Supabase
DATABASE_URL="postgresql://user:password@localhost:5432/whereat?schema=public"

# Production (Vercel will auto-populate this)
# DATABASE_URL="postgres://..."
